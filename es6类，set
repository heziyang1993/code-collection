```
class test {
      constructor () {
        this.name = 'yang'
      }
      plus (a, b) {
        return a + b
      }
    }
    console.log(new test().name)
    console.log(new test().plus(1, 2))
```
```
// set去重
var set = new Set([1, 2, 3, 4, 4]);
[...set] // [1,2,3,4]
```
